# Rendered by deploy.sh via envsubst using /opt/zhangyahan/school-timetable.env
# Do NOT put real secrets in this template — it is committed to git.

# Django
DEBUG=False
SECRET_KEY=${SECRET_KEY}
ALLOWED_HOSTS=${ALLOWED_HOSTS}

# CORS
CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}

# JWT
JWT_ACCESS_MINUTES=120
JWT_REFRESH_DAYS=7

# Logging
LOG_LEVEL=INFO
